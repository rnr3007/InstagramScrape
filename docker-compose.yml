version: '3'
services:
  automator_app:
    container_name: automator_app
    build: .
    env_file:
      - ./instagram-scrape/.env
    volumes:
      - ./instagram-scrape:/usr/src/app
    ports:
      - "3000:3000"
    